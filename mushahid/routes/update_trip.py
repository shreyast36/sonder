# TODO: Mushahid — POST /update-trip
# Accept: UpdateTripRequest (itinerary_id, user_id, feedback, current_itinerary)
# Verify Firebase Auth token.
# Call refinement/loop.py → UpdateTripResponse
# Push updated itinerary to Firestore so both users see changes live.
