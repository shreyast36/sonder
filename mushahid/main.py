# TODO: Mushahid — FastAPI app entry point.
# - Create FastAPI app with title "Sonder API", CORS middleware (allow Vercel frontend origin).
# - Lifespan: initialise Firestore client, Sentry, PostHog on startup.
# - Register all routers from routes/.
# - Run with: uvicorn mushahid.main:app --reload --port 8000
