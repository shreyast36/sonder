# TODO: Mushahid — Firestore client and pipeline state management.
# Initialise firebase_admin with credentials from shared/config.py.
# get_db() → firestore.Client
# write_itinerary_status(user_id, status: str) → None   (Generating → Ready)
# write_itinerary(itinerary: Itinerary) → None
# get_itinerary(itinerary_id) → Itinerary | None
