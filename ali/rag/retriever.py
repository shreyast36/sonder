# TODO: Ali — retrieve relevant context from Pinecone for RAG explanations.
# retrieve_activity_context(activity: Activity, top_k: int) → list[str]
#   Search Pinecone for reviews/descriptions related to this activity.
#   Uses Shreyas's search.py under the hood.
# retrieve_destination_context(destination: Destination, top_k: int) → list[str]
