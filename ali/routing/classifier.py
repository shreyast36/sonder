# TODO: Ali — request complexity and intent classifier.
# classify(task_type: str, context: dict) → ModelTier
#   Determine which model tier to use based on task type, token estimate, and latency budget.
# estimate_tokens(prompt: str) → int
#   Estimate token count for budget-aware routing decisions.
