# TODO: Ali — AI-generated chat topics and icebreakers (SMALL model tier).
# generate_topics(user_profile: UserProfile, match: CoTravellerMatch, itinerary: Itinerary) → list[str]
#   Route to SMALL model. Generate 5 conversation starters based on:
#   shared interests, itinerary overlap, compatibility signals, travel style.
# generate_icebreaker(user_profile, match) → str
#   Single suggested opening message for the chat screen.
