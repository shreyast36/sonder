# TODO: Ali — Module 6: itinerary generation via the routing engine.
# generate_itinerary(user_profile: UserProfile, destination: Destination, activities: list[Activity]) → Itinerary
#   Route to LARGE model tier.
#   Produce a day-by-day plan: balanced activities, respects pace preference,
#   stays within budget, uses persona + mood context.
#   Stream tokens back to Mushahid's SSE layer as they arrive.
