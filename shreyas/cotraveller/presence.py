# TODO: Shreyas — online presence tracking via Firestore.
# - set_online(user_id: str) → None       Write presence doc to Firestore.
# - set_offline(user_id: str) → None      Remove/update presence doc.
# - is_online(user_id: str) → bool        Read presence doc.
# - listen_presence(user_id, callback)    Firestore real-time listener for presence changes.
