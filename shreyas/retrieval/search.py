# TODO: Shreyas — Pinecone vector search functions.
# - search_destinations(user_profile, top_k) → list of Pinecone match dicts
# - search_activities(destination_id, user_profile, top_k) → list of Pinecone match dicts
# - search_cotravellers(user_profile, top_k) → list of Pinecone match dicts
# - upsert_destinations(destinations: list[Destination]) → None
# - upsert_activities(activities: list[Activity], destination_id) → None
# - upsert_cotraveller(profile: CoTravellerProfile) → None
# Filter by metadata type field: "destination" | "activity" | "cotraveller"
