# TODO: Shreyas — generate text embeddings via OpenAI text-embedding-3-small.
# - embed_text(text: str) → list[float]
# - embed_batch(texts: list[str]) → list[list[float]]
# - build_user_query(user_profile: UserProfile) → str
#   Concatenates destination type, pace, budget, persona interests, and mood
#   into a single descriptive string for embedding.
