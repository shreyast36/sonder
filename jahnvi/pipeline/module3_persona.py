# TODO: Jahnvi — Module 3: persona and emotion inference.
# - infer_persona(answers: PersonaQuestionAnswers) → dict
#   Classify user into a persona archetype (e.g. Cultural Explorer, Adventure Seeker).
# - infer_emotion(signals: dict) → EmotionIntent
#   Detect mood from input signals (text, selections, pace).
# - build_compatibility_signals(profile: UserProfile) → dict
#   Extract signals used by Shreyas's matching algorithm.
# - build_travel_style_embedding(profile: UserProfile) → list[float]
#   Generate embedding from persona + constraints for co-traveller vector search.
