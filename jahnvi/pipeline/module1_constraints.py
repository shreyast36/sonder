# TODO: Jahnvi — Module 1: capture and validate user trip constraints.
# - capture_constraints(raw_input: dict) → TripConstraints
#   Parse and validate: destination type, dates, budget, group size,
#   pace preference, must-haves list, avoid list.
