// TODO: Jahnvi — Screen 6: Approve / Deny match.
// Design ref: Figma screen 6 (match card, compatibility stats, Approve & Travel Together / Not a Match buttons).
// On approve → POST /api/chat/approve. On deny → POST /api/chat/deny.
// Both users must approve to create shared itinerary. Show live status via Firestore listener.
