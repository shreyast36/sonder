// TODO: Jahnvi — typed API client for the FastAPI backend.
// All requests attach Firebase Auth ID token in Authorization header.
// planTrip(userProfile) → EventSource (SSE stream)
// updateTrip(request)   → UpdateTripResponse
// getCotravellers(userId) → list[CoTravellerMatch]
// startChat(userId, profileId) → ChatSession
// approveMatch(sessionId, userId) → ApprovalStatus
// denyMatch(sessionId, userId)   → ApprovalStatus
