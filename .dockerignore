# Frontend — deployed separately on Cloudflare Pages.
# NOTE: only exclude jahnvi/frontend/. jahnvi/schemas/ is a shared Python
# module imported by the backend (shared/schemas.py does `from jahnvi.schemas...`).
jahnvi/frontend/

# Secrets — set via `fly secrets set`, never bake into image
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Virtual envs
.venv/
venv/
env/

# Git / IDE
.git/
.gitignore
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Build artifacts / logs
*.log
dist/
build/

# Tests / dev-only docs (optional — drop if you want them in the image)
tests/
*.md
